5
Выберите верное утверждение о переменных x и y:

4
x, y - локальные переменные
x - локальная, y - глобальная
x - глобальная, y - локальная
x, y - глобальные переменные

x - глобальная, y - локальная

Переменные, объявленные внутри функций называются локальными.

Переменные объявленные вне функций и других блоков называются глобальными. 

В данном случае x - это глобальная переменная, а y - локальная так как она объявлена внутри функции func.
